# Never track secrets or machine-specific configuration.
.zshsecrets
.zshrc.local
.gitconfig.local
.secrets/
*.token
*_rsa
*_ed25519
*.pem

# Neovim state that is generated, not configured. Plugins install into
# ~/.local/share/nvim; only the lock file belongs in version control.
.config/nvim/plugin/
.config/nvim/spell/
.local/share/

# Archives and binaries
*.tar.gz
*.zip

# macOS
.DS_Store
